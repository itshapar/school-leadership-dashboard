/**
 * Канали підтримки. Два різні за характером:
 *
 *   • Telegram — жива людина, коли щось горить прямо зараз;
 *   • форма — спокійне «знайшов баг» чи «хочу таку штуку», яке не губиться
 *     в переписці і до якого можна повернутись.
 *
 * Порожній рядок означає «каналу поки немає»: кнопка просто не показується.
 * Так підтримку можна викотити до того, як з'являться самі посилання, і не
 * ловити скарги на кнопку, яка нікуди не веде.
 *
 * Це константи, а не змінні оточення, свідомо: посилання публічні (їх і так
 * видно в розмітці), а зміна через код лишає слід у git, тоді як мовчазна
 * правка в дашборді Vercel не лишає нічого.
 */

/** Особистий Telegram, пряме звернення. Формат: https://t.me/username */
export const SUPPORT_TELEGRAM_URL = "https://t.me/ux_ui_ua";

/** Форма для багів і побажань. Формат: https://forms.gle/... */
export const SUPPORT_FORM_URL = "https://forms.gle/uVHmk63kYxkCdJux5";

export function hasSupportChannels(): boolean {
  return Boolean(SUPPORT_TELEGRAM_URL || SUPPORT_FORM_URL);
}
