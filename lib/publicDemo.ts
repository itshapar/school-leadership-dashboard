/**
 * Код постійного публічного демо-класу (Етап 9).
 *
 * Клас створено один раз напряму в БД (не через UI), позначено
 * classes.is_public_demo = true, і саме через public_class_overview він
 * доступний без реєстрації на /class/[code] — так само, як будь-який
 * реальний клас. /demo лише редіректить сюди.
 */
export const PUBLIC_DEMO_CLASS_CODE = "RCAC9FW68S";
